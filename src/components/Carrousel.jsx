import {useState} from 'react';

const colorChoice = ["purple", "blue", "green", "yellow", "orange", "red"];
export default function Carrousel(prop) {
    const 

    return (
      <>
       <button>Left</button>
       
       <button>Right</button>
      </>
    );
}